# Система учёта платежей (WPF + EF + MS SQL)

Приложение на WPF для ведения учёта платежей с ролевым доступом (Admin/User), авторизацией с капчей, сменой темы, ведением справочников (пользователи, категории, платежи) и экспортом/отчётностью через COM-объекты Office (Word/Excel).

Проект выполнен в рамках учебной практики по ПМ.01 «Разработка модулей программного обеспечения для компьютерных систем» и МДК 01.02 «Поддержка и тестирование программных модулей».

➡️ **Репозиторий:** `https://github.com/neftysha/UP` 
➡️ **БД:** `Kamenetskiy_Payment`  

👨‍💻 **Авторы проекта:**  
**Каменецкий Даниил Юрьевич, Мельников Сергей Владимирович**, студенты группы **4ИСиП-522**  

---

## Возможности

- авторизация по логину и паролю с хешированием (SHA1);
- ролевой доступ: **Admin** и **User**;
- переключение светлой/тёмной темы;
- работа со справочниками:
  - пользователи;
  - категории платежей;
  - платежи;
- фильтрация и сортировка пользователей;
- формы добавления/редактирования/удаления записей (CRUD);
- валидация данных (обязательные поля, числа > 0, корректная дата и т.п.);
- отображение диаграмм по платежам пользователя;
- экспорт/генерация отчётов (Word/Excel) через COM;
- аккуратный единый UI через словарь стилей.

---

## Технологии

- **.NET / WPF** — клиентское приложение;
- **Entity Framework** — доступ к данным;
- **Microsoft SQL Server** (SSMS) — хранение данных;
- **WindowsFormsHost + System.Windows.Forms.DataVisualization** — диаграммы;
- **COM-объекты Office** — отчёты (Word/Excel);
- **XAML стили** — единый внешний вид и темы.

---

## Структура базы данных

База данных: **`Kamenetskiy_Payment`**

Таблицы:

1. **Users**
   - `ID` (int, PK, Identity)
   - `Login` (varchar(max), not null)
   - `Password` (varchar(max), not null) — хранится хеш SHA1
   - `Role` (varchar(50)) — `Admin` или `User`
   - `FIO` (varchar(max), not null)
   - `Photo` (nvarchar(max), null)

2. **Category**
   - `ID` (int, PK, Identity)
   - `Name` (varchar(max), not null)

3. **Payment**
   - `ID` (int, PK, Identity)
   - `CategoryID` (int, FK → Category.ID)
   - `UserID` (int, FK → Users.ID)
   - `Date` (date, not null)
   - `Name` (nvarchar(max), not null)
   - `Num` (decimal(18,0), not null)
   - `Price` (decimal(18,0), not null)

Связи:  
- Users 1—* Payment  
- Category 1—* Payment

💾 В отчёте есть **полный SQL-скрипт** создания БД и таблиц — его можно выполнить в SSMS и сразу получить структуру и тестовые данные.

---

## Запуск проекта

1. **Создать базу данных**
   - открыть SQL Server Management Studio;
   - выполнить скрипт из папки `/sql` (создание БД `Kamenetskiy_Payment`, таблиц и заполнение тестовыми данными);
   - убедиться, что имена таблиц совпадают (`Users`, `Category`, `Payment`).

2. **Настроить строку подключения**
   - в проекте WPF открыть `App.config` / `appsettings.json` (как у тебя сделано) и прописать свой экземпляр SQL Server;
   - пример:
     ```xml
     <connectionStrings>
	<add name="Kamenetskiy_paymentEntities"
		 connectionString="metadata=res://*/Model1.csdl|res://*/Model1.ssdl|res://*/Model1.msl;
                       provider=System.Data.SqlClient;
                       provider connection string=&quot;
                       Data Source=USERHOME-PC45\SQLEXPRESS;
                       Initial Catalog=Kamenetskiy_payment;
                       Integrated Security=True;
                       TrustServerCertificate=True;
                       MultipleActiveResultSets=True;
                       App=EntityFramework&quot;"
		 providerName="System.Data.EntityClient" />
</connectionStrings>
     ```

3. **Собрать проект**
   - открыть решение в Visual Studio;
   - восстановить пакеты (Entity Framework и т.п.);
   - собрать.

4. **Запустить**
   - при запуске откроется главное окно с `Frame`;
   - по умолчанию приложение переходит на страницу авторизации.

---
