<Page x:Class="UP.Pages.ChangePassPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Смена пароля"
      Background="{DynamicResource AppBackground}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="Смена пароля"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,12"
                   Style="{DynamicResource HeaderText}"/>

        <!-- Карточка с полями -->
        <Border Grid.Row="1" Style="{DynamicResource Card}">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Логин -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:"
                           VerticalAlignment="Center" Margin="5"
                           Style="{DynamicResource BodyText}"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TbLogin"
                         Height="40" Margin="5"
                         Style="{DynamicResource RoundedTextBox}"/>

                <!-- Текущий пароль -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Текущий пароль:"
                           VerticalAlignment="Center" Margin="5"
                           Style="{DynamicResource BodyText}"/>
                <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="CurrentPasswordBox"
                             Height="40" Margin="5"
                             Style="{DynamicResource RoundedPasswordBox}"/>

                <!-- Новый пароль -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Новый пароль:"
                           VerticalAlignment="Center" Margin="5"
                           Style="{DynamicResource BodyText}"/>
                <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="NewPasswordBox"
                             Height="40" Margin="5"
                             Style="{DynamicResource RoundedPasswordBox}"/>

                <!-- Подтверждение -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Подтвердите пароль:"
                           VerticalAlignment="Center" Margin="5"
                           Style="{DynamicResource BodyText}"/>
                <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="ConfirmPasswordBox"
                             Height="40" Margin="5"
                             Style="{DynamicResource RoundedPasswordBox}"/>

                <!-- Требования -->
                <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                           Text="Требования к паролю:
• Минимум 6 символов
• Только английские буквы
• Хотя бы одна цифра"
                           Margin="5,10,5,0"
                           TextWrapping="Wrap"
                           Style="{DynamicResource CaptionText}"/>
            </Grid>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,16,0,0">
            <Button x:Name="ButtonSave"
                    Content="Сменить пароль"
                    Width="160" Margin="6,0"
                    Click="ButtonSave_Click"
                    Style="{DynamicResource PrimaryButton}"/>
            <Button x:Name="ButtonCancel"
                    Content="Отмена"
                    Width="120" Margin="6,0"
                    Click="ButtonCancel_Click"
                    Style="{DynamicResource OutlineButton}"/>
        </StackPanel>
    </Grid>
</Page>
